<div class="d-flex flex-column justify-content-center align-items-center p-10 mt-5">
      <%= image_tag ("img2.png") %>
    <div class="mt-3">
      <h1>My Budget App</h1>
    </div>
    <div class="d-flex m-2 gap-3 mt-3">
      <%= button_to "Sign In", new_user_session_path, method: :get, class: 'btn btn-outline-success' %>
      <%= button_to "Sign Up", new_user_registration_path, method: :get, class: 'btn btn-outline-primary' %>
    </div>
</div>